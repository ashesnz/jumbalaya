# Copy to .env for local overrides (optional).
# .env is gitignored and will never be committed.

# Example placeholders — this project does not currently read .env,
# but this file documents the pattern for future secrets.

# STEAM_APP_ID=2379780
# HTTP_API_KEY=
