# Copy to `.env` in the project root for local dev overrides.
# Shell environment variables take precedence over this file.

# Skip the first-play welcome tutorial (1, true, yes, on)
# SKIP_TUTORIAL=1

# Enable verbose engine logging and the FPS overlay in non-release builds (1, true, yes, on)
# VERBOSE=1
